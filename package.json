{
  "name": "cursor-sdk-server",
  "version": "0.3.1",
  "description": "Bun-based HTTP server wrapping the Cursor TypeScript SDK with a tRPC API",
  "type": "module",
  "bin": {
    "cursor-sdk-server": "./dist/cli.js"
  },
  "main": "./dist/server.js",
  "exports": {
    "./router": {
      "types": "./dist/router.d.ts",
      "import": "./dist/router.js"
    },
    "./schemas": {
      "types": "./dist/schemas.d.ts",
      "import": "./dist/schemas.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "SPEC.md"
  ],
  "scripts": {
    "build": "bun build ./src/cli.ts --outdir dist --target bun --external sqlite3 && bun build ./src/router.ts --outdir dist --target bun && bun build ./src/schemas.ts --outdir dist --target bun && bun run build:types",
    "build:types": "tsc --emitDeclarationOnly --declaration --declarationMap --outDir dist",
    "build:bundle": "bash scripts/build-release-bundle.sh",
    "prepare": "bun run build",
    "dev": "bun run src/cli.ts",
    "start": "bun run dist/cli.js",
    "test:e2e": "bun run scripts/e2e-test.ts",
    "test:install": "bash scripts/test-install.sh",
    "test:example": "bun run examples/client/index.ts"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.13",
    "@trpc/server": "^11.1.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@trpc/client": "^11.1.0",
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "license": "MIT"
}
